name: hqplugin
description: HelloHQ plugin CLI — build, test, and publish plugins.
version: 0.1.0
publish_to: none

environment:
  sdk: ">=3.4.0 <4.0.0"

executables:
  hqplugin: hqplugin

dependencies:
  args: ^2.5.0
  path: ^1.9.0
  ffi: ^2.1.0
  hellohq_plugin_mock_host:
    path: ../mock-host

dev_dependencies:
  test: ^1.25.0
  lints: ^4.0.0
